<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group userselect">
    <label for="order_user_id">Encontrista</label><br>
    <%=grouped_collection_select(:order, :user_id, Team.order('nome'), :users, :nome, :id, :nome) %>

  </div>

  <div class="form-group">
    <%= form.label :total %>
        <%= form.text_field :total, class:'form-control' %>
  </div>

  <div class="form-group">
    <label for="user_desconto">Pagamento</label><br>
  <input class="form-control" type="text" value="0.0" name="order[desconto]" id="order_desconto">
  </div>



  <div class="actions">
    <%= form.submit nil, :class => 'btn btn-outline-success' %>
  </div>
<% end %>
